% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis_functions.R
\name{makeLibraryNormalizedMatrix}
\alias{makeLibraryNormalizedMatrix}
\title{Generate a Library-Normalized Read Count Matrix for Genomic Regions}
\usage{
makeLibraryNormalizedMatrix(
  bamPath =
    "../01_Asynchronous_HCT116/results/mergedDownSampledBams/MTBP_WT_HCT116_anti-GFP_250.bam",
  Ranges,
  plottingRange = 4001,
  windowSize = 150,
  stepSize = 5,
  coreCount = 8
)
}
\arguments{
\item{bamPath}{Character string. Path to the BAM file.}

\item{Ranges}{A \code{GRanges} object representing the genomic regions of interest.}

\item{plottingRange}{Integer. Total width of the region around each peak (default: 4001).}

\item{windowSize}{Integer. Size of the smoothing window (default: 150).}

\item{stepSize}{Integer. Bin size for signal extraction (default: 5).}

\item{coreCount}{Integer. Number of parallel threads to use (default: 8).}
}
\value{
A matrix of CPM-normalized read counts (rows = positions, columns = regions).
}
\description{
This function computes a matrix of read counts across sliding windows
centered on specified genomic regions, normalized by library size (CPM).
The output is suitable for metaplot or heatmap visualization.
}
\examples{
peaks <- GRanges("chr1", IRanges(start = c(100000, 200000), width = 4001))
normMatrix <- makeLibraryNormalizedMatrix("example.bam", peaks)
}
