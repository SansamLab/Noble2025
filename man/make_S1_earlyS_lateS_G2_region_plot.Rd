% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_S1_earlyS_lateS_G2_region_plot.R
\name{make_S1_earlyS_lateS_G2_region_plot}
\alias{make_S1_earlyS_lateS_G2_region_plot}
\title{Create a multi-track genome browser-style plot for a genomic region}
\usage{
make_S1_earlyS_lateS_G2_region_plot(
  chromosomeToPlot,
  startToPlot,
  endToPlot,
  windowSize = 50000,
  stepSize = 5000,
  YLimits = c(-1.7, 1.4),
  Bams_Beds_and_Colors_List = list(G1 = list(txBamFile = "G1_tx.bam", inBamFile =
    "G1_in.bam", peaksBedFile = "peaks.bed", track_color = G1_Color), EarlyS =
    list(txBamFile = "EarlyS_tx.bam", inBamFile = "EarlyS_in.bam", peaksBedFile =
    "peaks.bed", track_color = EarlyS_Color), LateS = list(txBamFile = "LateS_tx.bam",
    inBamFile = "LateS_in.bam", peaksBedFile = "peaks.bed", track_color = LateS_Color),
    G2 = list(txBamFile = "G2_tx.bam", inBamFile = "G2_in.bam", peaksBedFile =
    "peaks.bed", track_color = G2_Color)),
  subregion_marker_color = "red"
)
}
\arguments{
\item{chromosomeToPlot}{Chromosome name as a character string (e.g., "chr1").}

\item{startToPlot}{Start position of the genomic region as a numeric value.}

\item{endToPlot}{End position of the genomic region as a numeric value.}

\item{windowSize}{Size of sliding window for signal smoothing (default: 50000).}

\item{stepSize}{Step size between windows (default: 5000).}

\item{YLimits}{Numeric vector of length 2 specifying y-axis limits for log2(Signal/Input).}

\item{Bams_Beds_and_Colors_List}{A named list of lists, one per condition (e.g., "G1", "EarlyS").
Each inner list must include:
\describe{
  \item{txBamFile}{Path to treatment BAM file.}
  \item{inBamFile}{Path to input BAM file.}
  \item{peaksBedFile}{Path to peaks BED file (unused in this function but included for completeness).}
  \item{track_color}{Color for the plotted track.}
}}

\item{subregion_marker_color}{Color of horizontal line marker to indicate subregion of interest (default: "red").}
}
\value{
A ggplot2 object composed using cowplot::plot_grid, combining genome tracks and a heatmap.
}
\description{
This function generates a genome browser-style figure showing log2 signal-to-background
ratios across a specified genomic region, along with a Repli-seq heatmap.
}
\details{
Each track represents a different cell cycle stage, using input and treatment BAM files
and optional peak annotations. The plot includes a subregion marker line and custom y-axis limits.
}
