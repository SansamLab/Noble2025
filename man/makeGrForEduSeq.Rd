% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/makeGrForEduSeq.R
\name{makeGrForEduSeq}
\alias{makeGrForEduSeq}
\title{Convert EduSeq Data Frame to GRanges Object}
\usage{
makeGrForEduSeq(Eduseqdata_df)
}
\arguments{
\item{Eduseqdata_df}{A data frame containing at least a column named \code{bin}.
Additional columns are preserved in the resulting GRanges object.}
}
\value{
A \code{GRanges} object with chromosome, start, end, and any extra metadata columns.
}
\description{
This function transforms an EduSeq-format data frame into a \code{GRanges} object.
It calculates genomic coordinates based on the `bin` column and filters to include
only standard human chromosomes (chr1–chr22).
}
\details{
The start coordinate is calculated as \code{(bin + 1) * 10000}, and the end
coordinate is set to \code{start + 1}. Only chromosomes chr1 through chr22 are retained.
}
\examples{
\dontrun{
df <- data.frame(bin = 0:4, seqnames = "chr1")
gr <- makeGrForEduSeq(df)
}
}
